---
paths:
  - "app/guardrails.py"
---

# 가드레일 규칙

- 새 가드레일은 입력(PII·범위)과 출력(유출) 양쪽을 모두 고려한다.
- 차단 시 사용자에게 보여줄 **이유 문자열**을 함께 반환한다(`Check(False, reason)`).
- **fail-closed**: 판정이 애매하면 통과가 아니라 차단으로 기운다.
- 정규식·키워드 휴리스틱은 stub임을 주석으로 명시하고, 결정적 테스트로 보강한다(Ch2).
