# 하네스 리뷰 — 2026-09-15

> `scripts/harness_review.sh` 가 만든다. 같은 체크리스트·같은 KPI·같은 프로브(결정적). 사람은 4) 결정만.
> 텔레메트리 근거: scripts/mock_otel_console.log

## 1) 자가점검 — 하네스 6종

| 대상 | 질문 | 판정 | 근거 |
|---|---|---|---|
| CLAUDE.md | 적힌 경로·규칙이 아직 맞나 | ⚠️ 갱신 | 경로 참조 9개 실재 대조 ✓ · 가드레일 한 곳 규칙 위반 후보: app/deploy_bot.py |
| 게이트 | 아직 걸리나 · 항상 경고만 하지 않나 | ✅ 유효 | harness_check PASS 6/6 · 스킬 grep 게이트 4줄 중 grep 2개 전부 OK 분기 도달 |
| 스킬 | 실제로 불리나 | ⚠️ 갱신 | skill_activated·command_name — code-review 1회 · adr 0회 → 0회: adr(죽은 무게 후보 — 로그 기간이 충분한가 먼저) |
| 훅 | 등록·실재·실행되나 · 왜 있는지 남았나 | ✅ 유효 | settings 8건 ↔ 스크립트 6개 실재·실행권한 ✓ · 흔적: BACKLOG 7건(append-backlog) · 작업로그 게이트 줄 3(gate-pytest) · hook_registered 8건(PostToolUse 1, PreToolUse 4, Stop 2, ConfigChange 1) ↔ settings 8건 |
| 서브에이전트 | 정의가 유효하고 호출되나 | ⚪ 근거 없음 | 정의 7개 · model 필드 전부 유효 · 로그 기간 내 호출 없음(agent.name 없음) — 리뷰 세션이 든 로그로 다시. 계속 0이면 25·26강 흐름이 끊긴 것 |
| 플러그인 | .claude/ 와 같은가 · bump 필요한가 | ✅ 유효 | version 1.1.0 · 드리프트 0 · 1.1.0 이후 .claude/ 변경 없음 |

결정 필요: 2건 — CLAUDE.md, 스킬 · 근거 없음 1건(텔레메트리 로그로 채울 것)

## 2) KPI — 두 단계

| 층 | KPI | 값 | 근거 |
|---|---|---|---|
| 프록시 | 적발률 | 2건 / 1 PR | 26강 집계 confirm 🔴 Important(고정 포맷이라 셀 수 있다) |
| 프록시 | 작업당 비용 | $0.402 / PR | cost.usage 합 $0.402 · 스킬별 code-review $0.402 |
| 프록시 | usage | 토큰 364,484 · 캐시 읽기 49% | token.usage(type) · skill_activated code-review 1 |
| 결과 | 오탐률 | 1/3 = 33% | 26강 verdicts refuted / 검증된 발견 — false positive 의 KPI 판 |
| 결과 | MTTD · MTTR | 미기록 | 장애 ADR 1건(0009-incident-response.md)에 `- 발생:`·`- 발견:`·`- 복구:` 시각이 없다 — 다음 장애부터 기록해야 산출된다 |

판정 규칙: 프록시만 좋아지고 결과(오탐률·MTTD·MTTR)가 안 움직이면 하네스가 헛도는 것. '미기록'은 0이 아니다 — 기록 형식부터.

## 3) 내구성 프로브

```
내구성 프로브 — 나쁜 것을 넣고 게이트가 막나 (전부 되돌림)
  P1 float in pension_calc           생존 ✓ (PASS 6/6 → PASS 5/6 · 스킬 grep 적중)
  P2 주민번호 in answer          생존 ✓ (check_output_leak 차단)
  P3 bare except in agent            생존 ✓ (스킬 grep 적중)
  P4 failing test → Stop           생존 ✓ (gate-pytest exit 2)
  P5 wide diff → PreToolUse        생존 ✓ (guard-diff-size exit 2)
게이트 생존 5/5
```

## 4) 결정 (사람)

- [ ] CLAUDE.md | 적힌 경로·규칙이 아직 맞나 | ⚠️ 갱신 | 경로 참조 9개 실재 대조 ✓ · 가드레일 한 곳 규칙 위반 후보: app/deploy_bot.py 
- [ ] 스킬 | 실제로 불리나 | ⚠️ 갱신 | skill_activated·command_name — code-review 1회 · adr 0회 → 0회: adr(죽은 무게 후보 — 로그 기간이 충분한가 먼저) 

## 5) 재배포

- 결정을 `.claude/` 에 반영 → `python3 scripts/build_plugin.py` → `.claude-plugin/plugin.json` version bump → push → 팀은 `/plugin update ai-product-harness@pension_qa-team`(28강)
- 다음 리뷰: 6개월 뒤 같은 명령. 사람 캘린더에만 걸면 깜빡한다(19강) — 30강에서 에이전트에게 맡긴다.
