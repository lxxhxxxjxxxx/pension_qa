# 작업로그 — 자율 실행용 (21강)

> 세션이 끊겨도 여기서 다시 시작한다. 상태는 대화가 아니라 이 파일과 커밋에 있다.

- 작업: `app/guardrails.py` 에 출력 가드레일 `check_output_disclaimer(answer) -> Check` 추가 — 연금 안내 답변에 전문가 확인 권고 문구(예: "전문가", "상담")가 없으면 차단(fail-closed). 기존 세 가드레일(`check_input_pii`·`check_input_scope`·`check_output_leak`)과 같은 `Check` 패턴을 따른다.
- 성공기준: `python3 -m pytest -q` 초록 — `tests/test_guardrails.py` 맨 아래 `test_output_disclaimer_*` 3건 포함
- 진행:
  - [x] 테스트 작성 — `tests/test_guardrails.py` 맨 아래 3건(문구 없음 → 차단 / 문구 있음 → 통과 / 차단 이유 문자열) · 커밋 `wip: disclaimer 가드레일 테스트(red)`
  - [x] 구현 — `check_output_disclaimer` (`_PII_PATTERNS` 아래, `check_output_leak` 다음에)
  - [x] 초록 확인 후 커밋 `feat: check_output_disclaimer 가드레일(green)` · 이 파일의 체크박스 갱신 — disclaimer 3건 green 확인 후 커밋. **전체 초록(성공기준)은 미달**: `tests/test_needs_human.py` 1건이 의도된 항상-실패라 아래 HITL 참고
- HITL(대기): `tests/test_needs_human.py::test_needs_human` 은 `assert False` 로 고정된 게이트 상한 실측용 테스트. tests/ 수정 금지라 코드로는 성공기준을 못 맞춘다 — 삭제·skip·유지 여부는 사람이 결정. 이번 변경은 문구 유무만 검사하고 `agent.py` 에 연결하지 않았으므로 금융 판단(공제·한도·수령 안내 결과) 변화 없음.
- HITL 경계: 이 가드레일이 공제·한도·수령 안내의 **결과**를 바꾸면 자동 통과가 아니라 사람 확인. 문구 유무만 검사하면 해당 없음. `agent.py` 파이프라인 연결은 이번 작업 범위 밖(연결하면 기존 답변이 전부 차단되므로 SYSTEM 프롬프트 수정과 함께 별도 작업).
- 이력:
- [17:05:18] ▶ autorun 시작 (컨테이너 밖 — --dangerously-skip-permissions 거부, allowlist 로 좁혀 실행)
- [17:06:32] 구현 — `check_output_disclaimer` 추가(`_DISCLAIMER_KEYWORDS` any-match · 빈 답변도 차단 · 차단 이유 문자열). PostToolUse black 훅이 같은 파일의 기존 줄도 재포맷(동작 변화 없음)
- [17:06:39] pytest — `tests/test_guardrails.py` 28 passed(disclaimer 3건 green) · 전체 49 passed / 1 failed = `tests/test_needs_human.py::test_needs_human`
- [17:06:39] ⛔ HITL — 성공기준(전체 초록)은 코드로 못 맞춤: `test_needs_human` 은 의도된 항상-실패 테스트이고 tests/ 수정 금지. 사람 결정 대기. 금융 판단 변경 없음
- [17:07:13] 커밋 `6836049` feat: check_output_disclaimer 가드레일(green) — 체크리스트 항목 소진. 남은 것은 HITL 1건뿐(위)
- [17:07:39] 게이트 차단 1/3 (stop_hook_active=false)
- [17:07:39] 게이트 응답 — 고칠 수 없음. 실패 1건은 `test_needs_human`(tests/ 수정 금지, 위 17:06:39 HITL 줄). 코드·테스트 변경 없이 사람에게 인계
- [17:08:00] 게이트 차단 2/3 (stop_hook_active=true)
- [17:08:07] ⛔ 게이트 3회 연속 미통과 — 자율 중단, 사람 확인 필요(HITL)
- [17:08:07] ■ autorun 종료 success · turns=24 · cost=$1.30 · 172s
