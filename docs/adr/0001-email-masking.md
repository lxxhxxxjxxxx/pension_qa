# ADR 0001 — 이메일은 차단이 아니라 마스킹

## 상태
채택 (2026-07)

## 맥락
출력 가드레일이 PII를 어떻게 다룰지. 이메일은 정상 답변(문의처 안내 등)에
포함될 수 있어 전면 차단 시 유용성이 떨어짐.

## 결정
이메일은 **마스킹**(`a***@d***.com`)한다. 주민번호·계좌는 기존대로 차단.

## 결과
- 정상 답변 유지 + PII 노출 감소.
- 트레이드오프: 마스킹 우회(이미지 등)는 범위 밖 — 추후 ADR.
