# SPEC — 근거 부실 게이트 (검색 점수 임계)

> 선행 결정: [ADR 0002 — 근거 부실은 2단 임계로 보류·저신뢰 구분](docs/adr/0002-evidence-score-gate.md)
>
> 후속 결정: [ADR 0003 — 검색 점수를 IDF 가중으로 바꾸고 임계값 재캘리브레이션](docs/adr/0003-idf-weighted-retrieval.md).
> 게이트 구조(2단 임계·3구간·역할 분리)는 그대로고 **커버리지 산식과 소프트 임계값**이 바뀌었다.
> 아래 수치는 ADR 0003 기준으로 갱신돼 있다.

## 목표

검색 근거가 부실하면 **답변하지 않거나(보류) 저신뢰임을 명시**한다.

현재 `app/agent.py`는 검색 결과가 **완전히 빈 경우에만** 차단한다.
`retriever.search()`는 토큰 겹침이 `score > 0`이기만 하면 문서를 돌려주고 점수는 버리므로,
질문 토큰 4개 중 1개만 겹친 문서로도 LLM이 호출되고 그 답변이 정상 답변(`✅`)과 구분 없이 나간다.
`.claude/rules/guardrails.md`의 fail-closed 원칙과 어긋나는 구간을 닫는다.

부수 목표: 하드 미달 시 **LLM 호출 자체를 생략**해 비용과 외부 전송을 줄인다.

## 범위

**대상**
- `app/retriever.py` — 점수를 살려 반환하는 경로 + 커버리지 산출 (점수 **계산**은 검색기 소관)
- `app/guardrails.py` — 임계 상수 + 3단 구간 **판정** 함수 (판정 로직은 가드레일 한 곳에만 — CLAUDE.md 컨벤션)
- `app/agent.py` — 게이트 분기, `Result.low_confidence` 필드 추가
- `app/main.py` — 저신뢰 접두사 렌더링
- `tests/test_guardrails.py` — 임계 판정 단위 테스트
- `tests/test_agent.py` — 신규, 파이프라인 통합 테스트

**범위 밖**
- 검색 알고리즘 교체(임베딩·형태소 분석) — 게이트는 현재 토큰 겹침 스코어 **위에** 얹는다. 검색기 개선은 README TODO로 남긴다
  → 이후 ADR 0003에서 스코어를 IDF 가중으로 교체했다(임베딩·형태소 분석기는 여전히 미도입). 게이트 구조는 손대지 않고 임계값만 재측정했다
- 임계값의 환경변수·런타임 설정화 — 상수로 하드코딩
- 저신뢰 구간에서 LLM 프롬프트를 바꾸는 것 — `app/llm.py`의 SYSTEM은 무변경
- 점수 수치를 사용자에게 노출하는 것 — 문서 이름까지만 노출한다
- 로깅·메트릭 계층 — 현재 별도 로깅 코드가 없어 미래 대비 코드가 되므로 제외(`SPEC.md`와 동일한 이유)
- 이메일 마스킹·PII 차단 로직 변경 — 무변경. 회귀 테스트로만 보호한다
- 입력 범위(scope) 판정 강화 — 이 게이트는 scope 통과 **이후**에만 동작한다

## 동작

### 구간 정의

```
커버리지 = Σ idf(검색된 문서들이 커버한 질문 토큰) / max(Σ idf(질문 토큰), idf(df=1))

0.00 ─── 보류 ─── 0.15 ── 저신뢰 ── 0.50 ─── 정상 ─── 1.00
```

| 구간 | 조건 | 동작 |
|---|---|---|
| 보류 | `< 0.15` | `blocked=True`, `sources`는 **채운다**(문서 이름만), `llm.answer()` **미호출** |
| 저신뢰 | `0.15 ≤ x < 0.50` | 평소대로 답변하되 `low_confidence=True` |
| 정상 | `≥ 0.50` | 현행과 동일 |

경계는 `>=` 통과 — 정확히 `0.15`면 저신뢰, 정확히 `0.50`이면 정상.
fail-closed 원칙에서 경계 한 칸만 양보하며, 부등호 방향은 테스트로 고정한다.

### 임계값 캘리브레이션 근거

현재 `data/` 3개 문서에 21개 질문을 실측해 잡았다. 전체 분포는
[ADR 0003](docs/adr/0003-idf-weighted-retrieval.md)에 있고, 여기서는 구간 경계만 옮긴다.

| 질문 | 커버리지 | 구간 |
|---|---|---|
| 퇴직 위로금 협상 노하우 알려줘 | 0.000 | 검색 결과 없음 |
| 퇴직금 받았는데 IRP로 옮기면 세금 어떻게 되나요? | 0.087 | 보류 |
| 연금저축 관련해서 요즘 날씨랑 점심 메뉴… | 0.125 | 보류 |
| 연금 | 0.136 | 보류 |
| ← 0.15 경계 | | |
| IRP 중도인출 되나요 | 0.324 | 저신뢰 |
| IRP 운용 상품 추천해줘 | 0.425 | 저신뢰 |
| ← 0.50 경계 | | |
| 연금 해지하면 어떻게 되나요 | 0.532 | 정상 |
| 연금소득세 과세 어떻게 되나요 | 0.532 | 정상 |
| 연금저축 세액공제 한도가 얼마인가요? | 0.662 | 정상 |
| IRP 수령 요건 알려줘 | 0.662 | 정상 |

절대 겹침 개수가 아니라 **비율**을 쓰는 이유: 질문이 길수록 겹침 개수가 자연히 커져
같은 절대 임계가 짧은 질문에는 가혹하고 긴 질문에는 무력해진다.
개수 비율이 아니라 **IDF 가중** 비율을 쓰고 분모에 **하한**을 두는 이유는 ADR 0003 참고 —
순수 개수 비율에서는 `"연금"` 한 단어가 1.00으로 게이트를 통과했다.

### 파이프라인

```
질문 → check_input_pii → check_input_scope
     → retriever 검색(점수 유지)
     → 빈 결과? → 기존 문구 그대로 보류
     → 커버리지 산출 → guardrails 임계 판정
         ├ 보류   → Result(reason, sources=[이름], blocked=True)   ← LLM 미호출
         ├ 저신뢰 → llm.answer → check_output_leak → mask_emails → Result(..., low_confidence=True)
         └ 정상   → llm.answer → check_output_leak → mask_emails → Result(...)
```

`SPEC.md`의 입력 마스킹이 적용된 뒤라면 게이트는 **마스킹된 질문** 기준으로 커버리지를 계산한다
(검색에 들어가는 문자열과 게이트가 채점하는 문자열이 항상 같아야 한다).

### 사용자에게 보이는 모양

- 보류: `⛔` + 보류 사유 + `근거: <문서 이름들>`
  — "답은 못 드리지만 이 문서들이 관련 있어 보입니다". 기존 출력 유출 차단 경로가 이미 `sources`를 채우는 것과 일관되고, CLAUDE.md의 "응답 경로는 `Result.sources`를 채운다" 컨벤션에도 맞는다.
- 저신뢰: `⚠️` + 답변 + `근거: …`
- 정상: `✅` + 답변 + `근거: …`

점수 수치는 어느 구간에서도 노출하지 않는다.

### 구조 결정

- 커버리지 **계산**은 `retriever.py`, 임계 **판정**은 `guardrails.py`.
  가드레일 로직을 한 곳에 둔다는 컨벤션을 지키면서, 검색기 내부 표현(토큰 집합)이 가드레일로 새지 않게 한다.
- 판정 결과는 3단이라 기존 2값 `Check(ok, reason)`으로 표현하지 않는다.
  별도 결과 타입으로 구간을 돌려주되, 보류 사유 문자열은 가드레일 규칙대로 함께 반환한다.
- `retriever.search()`의 기존 시그니처·반환 타입은 유지한다(`tests/test_retriever.py`가 의존).
  점수가 필요한 호출부만 새 경로를 쓴다.
- 저신뢰 표시는 `answer` 문자열이 아니라 `Result.low_confidence` 필드.
  LLM이 생성한 본문을 건드리지 않고, 후속 처리·테스트가 문자열 매칭에 의존하지 않게 한다.

### 알려진 한계

- 토큰 겹침 기반이라 커버리지는 여전히 의미 유사도가 아니다. 어휘가 어긋나면 답이 문서에 있어도 못 찾는다 — `"연금 나이 제한 있나요"`는 문서가 `연령`·`만 55세`로 쓰고 있어 0.043(보류)이다.
- ~~`"연금"` 한 단어 질문이 커버리지 1.00으로 통과~~ → ADR 0003의 IDF 가중 + 분모 하한으로 0.136(보류)이 됐다.
- ~~조사가 붙은 토큰(`한도가` vs `한도`)이 안 겹친다~~ → ADR 0003의 접사 정규화로 해결.
- 임계값은 현재 `data/` 3개 문서 기준. 문서가 늘거나 검색기가 바뀌면 재측정해야 한다.

## 검증 (통과 기준)

가드레일 규칙에 따라 **테스트를 먼저 추가**하고 구현한다.

### 임계 판정 단위 — `tests/test_guardrails.py`

| # | 케이스 | 기대 |
|---|---|---|
| 1 | 커버리지 `0.0` | 보류, 사유 문자열이 비어 있지 않음 |
| 2 | `0.14` (하드 바로 아래) | 보류 |
| 3 | `0.15` (하드 경계) | 저신뢰 |
| 4 | `0.49` (소프트 바로 아래) | 저신뢰 |
| 5 | `0.50` (소프트 경계) | 정상 |
| 6 | `0.75` | 정상 |
| 6b | `0.40` (ADR 0002의 옛 소프트 경계) | 저신뢰 — 재캘리브레이션이 되돌려지지 않게 고정 |

### 파이프라인 통합 — `tests/test_agent.py` (신규)

`llm.answer`를 monkeypatch해 **호출 횟수**를 센다.

| # | 케이스 | 기대 |
|---|---|---|
| 7 | `"연금저축 관련해서 요즘 날씨랑 점심 메뉴…"` (≈0.13) | `blocked=True`, `sources` 비어 있지 않음, `llm.answer` 호출 **0회** |
| 7b | `"연금"` (≈0.14) | `blocked=True`, LLM 호출 **0회** — ADR 0003이 닫은 구멍 |
| 8 | `"IRP 중도인출 되나요"` (≈0.32) | `blocked=False`, `low_confidence=True`, `sources` 채워짐, LLM 1회 호출 |
| 9 | `"IRP 수령 요건 알려줘"` (≈0.66) | `blocked=False`, `low_confidence=False`, `sources` 채워짐 |
| 9b | `"연금 해지하면 어떻게 되나요"` (≈0.53) | 정상 — ADR 0002의 저신뢰에서 올라온 질문 |
| 10 | `"퇴직 위로금 협상 노하우 알려줘"` (검색 결과 없음) | 기존 문구 `"관련 근거 문서를 찾지 못해 답변하지 않습니다."` 유지 |
| 10b | `"퇴직금 받았는데 IRP로 옮기면 세금 어떻게 되나요?"` (≈0.09) | `blocked=True` + `sources` 채워짐. 검색기 개선으로 빈 결과 → 근거 노출 보류로 이동 |

### 회귀

| # | 케이스 | 기대 |
|---|---|---|
| 11 | PII 포함 질문 | 게이트 도달 전 기존대로 차단 |
| 12 | 이메일 포함 답변 | `mask_emails` 동작 무변경 |
| 13 | `retriever.search()` 직접 호출 | 기존 시그니처·반환 유지 (`tests/test_retriever.py` green) |

### 전체 기준

- `python -m pytest -q` 전부 green
- 임계 판정 로직은 `app/guardrails.py` 한 곳에만 (CLAUDE.md 컨벤션)
- 보류·저신뢰 포함 모든 응답 경로가 `Result.sources`를 채움 (검색 결과가 실제로 없는 10번 제외)
- CLI에서 세 구간이 서로 다른 접두사(`⛔` / `⚠️` / `✅`)로 구분됨
