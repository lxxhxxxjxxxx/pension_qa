# SPEC — 이메일 마스킹 가드레일

> 선행 결정: [ADR 0001 — 이메일은 차단이 아니라 마스킹](docs/adr/0001-email-masking.md)

## 목표

이메일 주소가 파이프라인을 통과할 때 **차단하지 않고 부분 마스킹**한다.
"문의처 안내" 같은 정상 답변의 유용성을 유지하면서 PII 노출을 줄이고,
사용자가 질문에 적어 넣은 이메일이 **외부 LLM API로 전송되지 않도록** 한다.

## 범위

**대상**
- `app/guardrails.py` — `mask_emails()` (기존 함수 재사용, 시그니처 변경 없음)
- `app/agent.py` — 입력 경로에 마스킹 추가. 검색·LLM 호출 전에 질문을 마스킹한 값으로 대체
- `app/agent.py` — 출력 경로 마스킹 유지 (현행)

**범위 밖**
- 로깅·CLI 출력 계층 — 현재 별도 로깅 코드가 없어 미래 대비 코드가 되므로 제외
- 주민번호·계좌 차단 로직 — 변경 없음
- 마스킹 수준 변경 — `h***@e***.com` 현행 유지
- 이미지·첨부 등 텍스트 외 경로 우회 — ADR 0001에 명시된 대로 추후 ADR

## 동작

### 마스킹 규칙 (변경 없음)

```
hong@example.com  →  h***@e***.com
```

로컬파트 첫 글자 + `***` + `@` + 도메인 첫 글자 + `***.` + TLD.

### 파이프라인

```
질문 → check_input_pii → check_input_scope → mask_emails ─┐
                                                          ↓
                                      retriever.search / llm.answer
                                                          ↓
                                      check_output_leak → mask_emails → Result
```

- 입력 마스킹은 **PII·범위 체크 통과 후**에 수행한다. 순서를 바꾸면 마스킹된 문자열이 PII 패턴 판정을 흐린다.
- 마스킹된 질문이 `retriever.search()`와 `llm.answer()` 양쪽에 전달된다. 원문 질문은 그 지점 이후로 흐르지 않는다.
- `Result.sources`(문서 이름)는 마스킹 대상이 아니다.
- 차단 경로(`blocked=True`)의 `reason` 문자열은 고정 문구라 마스킹 불필요.

### 구조 결정

이메일은 `_PII_PATTERNS`에 흡수하지 않고 `_EMAIL_PATTERN` + `mask_emails()`로 **분리 유지**한다.
차단(주민번호·계좌)과 마스킹(이메일)은 조치가 다르며, ADR 0001의 결정이 코드 구조에 그대로 드러나야 한다.

### 알려진 한계

- 정규식 휴리스틱이므로 `report@v2.tar.gz` 같은 `@`+점 조합은 오탐 가능. 가드레일 규칙상 fail-closed(과하게 마스킹) 방향이므로 허용한다.
- 입력 마스킹은 이메일이 검색 키워드로 쓰이는 질문의 리콜을 떨어뜨릴 수 있다. 아래 검증 항목으로 회귀를 감시한다.

## 검증 (통과 기준)

가드레일 규칙에 따라 **테스트를 먼저 추가**하고 구현한다. 전부 `tests/test_guardrails.py`.

| # | 케이스 | 기대 |
|---|--------|------|
| 1 | 기존: `"문의는 hong@example.com 으로"` | `"문의는 h***@e***.com 으로"` |
| 2 | 기존: 이메일 없는 답변 | 문자열 무변경 |
| 3 | 한 문장에 이메일 여러 개 — `"a@x.com 또는 b@y.com"` | 둘 다 마스킹 (`sub` 전역 치환 보장) |
| 4 | 다중 점 도메인 — `hong@mail.co.kr` | `h***@m***.kr` (TLD만 보존) |
| 5 | 짧은 로컬파트 — `a@b.com` | `a***@b***.com`, 인덱스 에러 없음 |
| 6 | 오탐 방지 — `"2026년 7@8 회차"`, `"@연금팀"` | 무변경 (TLD 없는 `@`는 이메일 아님) |
| 7 | 입력 마스킹 적용 — 이메일 포함 질문으로 `ask()` 호출 | `llm.answer()`에 전달된 질문에 원문 이메일 없음 |
| 8 | 입력 마스킹 후 검색 품질 — 이메일 포함 연금 질문 | 이메일 제거 전과 동일한 문서를 찾음 (리콜 회귀 방지) |

**전체 기준**
- `python -m pytest -q` 전부 green
- 새 가드레일 로직은 `app/guardrails.py` 한 곳에만 (CLAUDE.md 컨벤션)
- 응답 경로가 `Result.sources`를 계속 채움
