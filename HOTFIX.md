# HOTFIX.md — 급할 때 이 카드부터 (5줄)

1. 지킬 제약이 CLAUDE.md에 있는지 30초 확인 — 없으면 지금 한 줄 추가
2. 본작업 직전 수동 `/compact` — 남길 것(제약·결정·다음 할 일) 지정
3. 이 핫픽스의 최소 게이트 1개 지정 (예: `python -m pytest tests/test_guardrails.py -q`)
4. 결정·이유는 커밋 메시지에 (예: "가드레일 fail-closed 회귀 핫픽스 — 검색 실패 시 차단으로 되돌림")
5. 꼬이면 `/rewind` — 제약이 살아있던 지점으로

> 19강 산출물. 완화 5종을 급할 때 머리 대신 꺼내 쓰는 절차 카드.
> 근거: 루트 CLAUDE.md는 compaction 뒤에도 재주입되므로 핵심 제약은 대화가 아니라 파일에 둔다.
